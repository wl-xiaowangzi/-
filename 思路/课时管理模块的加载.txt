1. 给课程管理列表中的每一个行数据的"编辑课时"按钮绑定单击事件，在事件回调函数中：
    2. 实现从课程管理列表模块，跳转到课时管理的模块：
        实现思路：不能直接跳转，因为在课时管理列表中需要根据课程id获取指定课时信息，需要首先解决获取课程id的问题
        解决方案：
            a、点击编辑课时的时候，首先获取课程id，将它存放在"课时管理"的菜单按钮中
                -->通过给按钮添加一个属性(cs_id:课程id)
            b、实现从课程列表跳转到课时管理
                -->触发"课时管理"按钮的单击事件
            c、在"课时管理"单击事件中，正式的加载课时管理模块的代码
                -->获取课程id(通过获取"课时管理"按钮中保存的cs_id属性获取到课程id)
                -->通过ajax请求数据，根据上一步获取的课程id来获取指定的课时信息
                    -->$.get("/api/course/lesson",{cs_id:课程id},function(res){

                        -->接口成功的时候，也就是获取了课时信息
                            -->准备了一个"courseTimeList.html"模板文件
                            -->使用artTemplate编译模板，成功就会返回一个字符串
                                -->art.render(模板文件,res)
                            -->接上步，将该字符串转换为jquery对象
                            -->将该jquery对象放到页面中的指定位置
                    })